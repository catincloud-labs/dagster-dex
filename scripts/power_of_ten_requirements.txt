# ADR-0042 (constellation), rule 10: the tools that read the standard are
# pinned by version, ONCE, here - and the pin has a mover. This file is the one
# place the version is written; this repository's dependabot.yml reads this
# directory under the `pip` ecosystem, so a bump is a pull request here, read
# as a diff of a version and a changelog, and reaches a consumer only when its
# workbench commit pin moves past it (ADR-0040's mover). A version written into
# a `run:` step is in no ecosystem and has no mover - the frozen `mypy==1.14.1`
# the record measured is the shape this file exists to refuse.
#
# Both versions are the ones the record's readings were taken with, so the
# day-one counts it prices are the counts this action reports.
ruff==0.16.6
mypy==2.3.1
