# CLAUDE.md

The operating rules for this repository are in `AGENTS.md`. This file exists
because Claude Code loads `CLAUDE.md` automatically and does not load
`AGENTS.md` - so without it, the rules that govern this repo would sit in a
file nothing reads.

The line below is an import, not a citation. `AGENTS.md` stays the single
source; this file must never grow a second copy of anything in it, because two
copies of a rule is one rule and one stale claim.

@AGENTS.md
